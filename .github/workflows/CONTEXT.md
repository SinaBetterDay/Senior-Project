# GitHub Actions — FAIR (`.github/workflows/`)

## Role

CI workflows for the monorepo. Current artifact may use a nonstandard extension (e.g. `ci-cdyml.txt`); normalize to `.yml` when wiring real pipelines.

## Related docs

- `../../workflows/CONTEXT.md`
- `../../CLAUDE.md`
