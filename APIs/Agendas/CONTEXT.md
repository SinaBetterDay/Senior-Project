# Agendas — scripts (`APIs/Agendas/`)

## Role

City **agenda** import experiments — e.g. **Legistar** HTTP calls (`import requests.py`). Behavior here should align with `docs/fair/agenda-ingestion/CONTEXT.md` when ported to production **Node** services.

## Related docs

- Parent: `../CONTEXT.md`
- Ingestion overview: `../../docs/fair/agenda-ingestion/CONTEXT.md`
