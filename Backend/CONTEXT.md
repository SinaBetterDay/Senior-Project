# Backend (legacy folder) — FAIR

## Role

Miscellaneous or experimental backend bits. The **primary** Node API for FAIR lives in **`server/`** at the repo root.

## Guidance

- Prefer adding new Express/Prisma code under **`server/`** unless this folder is intentionally reserved for a specific experiment.
- If you consolidate or delete this folder, update `CLAUDE.md` and `DIRECTORY.md`.
