# Copy to .env and adjust. Only VITE_* variables are exposed to the browser.

# Base URL of the FAIR Express API (server/). Default matches `npm run dev` in server/.
VITE_API_URL=http://localhost:3001
