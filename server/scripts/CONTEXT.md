# Server scripts — FAIR (`server/scripts/`)

## Role

One-off or maintenance **Node** scripts (e.g. storage smoke tests). Not the main HTTP app — see `../CONTEXT.md` for Express.

## Related docs

- `../CONTEXT.md`
