# Supabase migrations — FAIR (`supabase/migrations/`)

## Role

**Versioned SQL** for schema, RLS, extensions, and Storage-related DDL. Apply in order; never rewrite history on shared branches.

## Related docs

- `../CONTEXT.md`
