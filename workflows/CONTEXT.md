# Workflows — FAIR (`workflows/`)

## Role

CI/CD or automation definitions (e.g. GitHub Actions YAML). Keep pipeline steps aligned with **Vercel** (client) and **Railway** (server) deploy assumptions.

## Related docs

- Root: `CLAUDE.md`
- Client/server: `client/CONTEXT.md`, `server/CONTEXT.md`
